// Types
import { SidebarMapping } from '@shared/types';

// Constants
import { ROUTE } from '@shared/constants';

export const SIDEBAR_MAPPING: SidebarMapping[] = [
  {
    heading: 'Dashboard',
    contentLink: 'Dashboard',
    pathRouter: ROUTE.DASHBOARD
  }
];
